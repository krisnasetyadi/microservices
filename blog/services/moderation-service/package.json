{
  "name": "moderation-service",
  "version": "1.0.0",
  "description": "when comment emit event it will emiting to event bus\r then it will emiting to moderation service to change it status\r then from moderation service it will emiting to event bus and from event bus\r then passing it to query service",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
